import { ReplaceEmptyPipe } from './replace-empty.pipe';

describe('ReplaceEmptyPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceEmptyPipe();
    expect(pipe).toBeTruthy();
  });
});
